package repository

import (
	"testing"
)

func TestReository(t *testing.T) {
	t.Run("Test NewRepository", func(t *testing.T) {
		// conf := config.New()
		// ctx := context.Background()

		// repo, err := NewRepository(ctx, conf.DbConfig.ConnectionUrl())
		// repo, err := (Repository, nil)
	})
}
