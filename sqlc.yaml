version: "1"
packages:
  - name: "repository"
    emit_json_tags: false
    emit_prepared_queries: true
    emit_interface: false
    path: "/profiler/repository"
    queries: "./profiler/repository/queries/"
    schema: "./profiler/repository/migrations/"

